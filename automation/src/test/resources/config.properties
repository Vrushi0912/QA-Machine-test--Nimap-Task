# ============================================
# Environment configuration
# ============================================
baseUrl=https://test.fieldforceconnect.com/
browser=chrome
implicitWait=10
explicitWait=15

# NOTE: Never commit real credentials to a public GitHub repo.
# For the assignment, either:
#  1) Read credentials from environment variables (recommended), or
#  2) Keep this file out of git via .gitignore and share a config.properties.sample instead.
username=your_test_username
password=your_test_password
